# SonarQube development setup

This library uses the SonarQube server at <https://sonar.casadega.dev>. The project key is
`reggieofarrell_http-client`. Keep `sonar-project.properties`, `.sonarlint/connectedMode.json`, and
the VS Code `sonarlint.connectedMode.project` binding in lockstep if that key ever changes.

If the project is created via the GitHub import on the server, paste the generated key into those
three files before the first analysis. A GitHub-imported key is preferred for pull-request
decoration with the Community branch plugin.

## Layers

- **Local ESLint:** every active server rule implemented by the installed `eslint-plugin-sonarjs` is
  a blocking error on production `src/` (tests and scripts stay out of ESLint, as before).
- **Agent post-edit hook:** a type-independent subset of that profile, generated by RuleSync.
  Findings fail the hook.
- **Pre-push precheck:** the real Scanner over files changed vs `origin/main`. Exit 2 (Scanner,
  credentials, or server unavailable) is a loud skip; findings and analysis failures still block.
- **CI:** the Tests workflow uploads Jest LCOV, then calls the public
  [`Casadega-Development/action-workflows`](https://github.com/Casadega-Development/action-workflows)
  reusable scan. The server gate is **new-code-only** because this is a mature library, not a
  greenfield starter. Coverage in Sonar is informational; `jest.config.js` `coverageThreshold`
  remains the coverage authority.

Neither the local plugin nor SonarQube for IDE replaces the server's complete analysis.

## CI setup

Create the project on the SonarQube server and configure these GitHub Actions values at the
**repository** level (avoid inheriting a different organization default):

| Name             | Kind     | Value                                                                    |
| ---------------- | -------- | ------------------------------------------------------------------------ |
| `SONAR_TOKEN`    | Secret   | **Project analysis token** for this project key (not a user/login token) |
| `SONAR_HOST_URL` | Variable | `https://sonar.casadega.dev`                                             |

Attach a quality gate whose conditions are on **new code**, not “any open issue.”

CI must use a **project analysis token** stored as the `SONAR_TOKEN` Actions secret. A SonarQube
**user** token (what `sonar auth login` puts in the macOS keychain for local `sonar:precheck`) is a
different kind: it authenticates a person, not this project. Either kind can analyze if it has
Execute Analysis, but the GitHub secret should be the project token so it is scoped to this project
and can be rotated without affecting developer logins.

The Tests workflow:

- runs on pull requests to `main` and on pushes to `main` (the latter re-baselines new code).
  `cancel-in-progress` applies only to pull requests so a `main` upload is not cancelled;
- runs the Sonar job on same-repo pull requests and on pushes to `main` (main already has a
  baseline analysis for the Community branch plugin). Fork PRs skip Sonar because GitHub does not
  expose repository secrets to those runs;
- calls `Casadega-Development/action-workflows/.github/workflows/sonar-scan.yml@main` and maps
  `SONAR_TOKEN` explicitly (`secrets: inherit` does not cross from this personal repository into the
  Casadega org; an inherited token arrives empty and the scanner returns HTTP 401);
- restores the `coverage` artifact from the Tests job;
- supplies explicit PR or branch parameters for the Community branch plugin;
- enforces `issue-gate-scope: new-code` plus the official quality-gate action
  (`sonarsource/sonarqube-quality-gate-action@v1.2.1`);
- upserts a sticky pull-request comment (`<!-- casadega-sonarqube -->`) on PR scans. Pushes to
  `main` do not comment.

The caller job grants `pull-requests: write` so that comment can be posted; missing permission fails
the comment step closed. Turn the comment off with `post-pr-comment: false` on the reusable workflow
inputs if a future caller does not want it.

Cloudflare in front of `https://sonar.casadega.dev` must skip Bot Fight / Managed Challenge for
scanner paths (`/batch*`, especially `/batch/project.protobuf`, and `/api/ce/submit`). A challenge
HTML 403 is reported by the scanner as “not authorized to analyze this project.”

The full CI scan is authoritative. Fork PRs still run lint, types, tests, build, and audit.

## Manual re-scan

The `SonarQube Re-scan` workflow can analyze an already tested commit without rerunning the entire
quality pipeline. Open a completed `Tests` run, copy the numeric run ID from its Actions URL, and
supply it to the manual workflow. It is a thin caller of
`Casadega-Development/action-workflows/.github/workflows/sonar-scan-after-tests.yml@main`, which
restores that run's coverage artifact and derives the original PR or branch context before scanning.

## Local command-line setup

Install both SonarQube Scanner and SonarQube CLI. On macOS, Scanner is available through Homebrew:

```bash
brew install sonar-scanner
```

Install SonarQube CLI with SonarSource's installer, then authenticate once to the configured server.
Always include `--server`; omitting it selects SonarQube Cloud EU rather than this repository's
server:

```bash
curl -o- https://raw.githubusercontent.com/SonarSource/sonarqube-cli/refs/heads/master/user-scripts/install.sh | bash
sonar auth login --server https://sonar.casadega.dev
```

On macOS, the CLI stores credentials in the keychain under service `sonarqube-cli`, keyed by host.
The precheck prefers the entry for `sonar.casadega.dev` over an inherited `SONAR_TOKEN` because the
keychain entry has a verifiable server identity. If a credential for another Sonar server is
already active, add or update this host without exposing the token in process arguments or shell
history:

```bash
security add-generic-password -U -s sonarqube-cli -a sonar.casadega.dev -w
```

The value-less `-w` prompts without echo, and `-U` makes the command safe to repeat. Do not append
the token to that command. `sonar auth status` shows the CLI's one active connection; stored host
entries can coexist even though only one connection is active. The host-scoped entry is sufficient
for `sonar:precheck`. When `sonar:rules` needs live profile data, deliberately activate this server
with the login command above and verify `sonar auth status`; changing servers changes the CLI's
active connection. The sync checks that active server against the committed host before issuing any
hostless `sonar api` query, so an empty result from the wrong server is never accepted as clean.

On non-macOS platforms, the local precheck has no verified credential-store adapter and uses
`SONAR_TOKEN` only. Supply a SonarQube **user token** without putting it in shell history, then
remove it when the session ends:

```bash
read -rs SONAR_TOKEN
export SONAR_TOKEN
printf '\n'
# Run the required command, then remove the session credential.
unset SONAR_TOKEN
```

Do not reuse the CI project-analysis token locally: the precheck also calls user-facing issues and
branch APIs. Never print a keychain token with `security find-generic-password ... -w`; the
precheck captures that command's output internally without writing the secret to the terminal.

For all repository-local Sonar tooling, the committed `sonar.host.url` is the only server
authority. An inherited `SONAR_HOST_URL` is never used as an override or fallback; a conflicting
non-blank value is reported and ignored. Missing committed host configuration blocks instead of
becoming a skippable precheck prerequisite. Profile-synchronization Web API calls go through the
authenticated SonarQube CLI, and synchronization fails if the CLI's active server does not match
the repository. This prevents a shell configured for another server from silently publishing
source or syncing the wrong profile.

```bash
npm run sonar:rules
npm run sonar:precheck
npm run sonar:precheck -- --all
npm run sonar:precheck -- another-base-branch
```

`sonar:rules` intersects active server rule IDs with rules implemented by the installed
`eslint-plugin-sonarjs`. Before the project is provisioned it uses the server-default profiles;
afterward it automatically uses profiles associated with the project. `--bootstrap` remains the
offline fallback from the plugin's recommended configuration.

The normal precheck reports issues introduced compared with `origin/main`. `--all` includes
pre-existing issues in every changed file. The script uses a short-lived server branch and removes
it after reading the findings.

Never put Sonar tokens in source files, committed environment files, command arguments, shell
history, or logs.

## Git hook behavior

Husky installs three complementary hooks during `npm install`:

- `pre-commit` runs the SonarQube CLI secret scan, then lint-staged.
- `commit-msg` enforces Conventional Commits through commitlint.
- `pre-push` runs the outgoing secret scan, the changed-file SonarQube analysis, then
  `rules:check` and `npm test`.

Secret scan failures always block. The changed-file server analysis exits with status 2 when
Scanner, credentials, the base branch, or the server is unavailable; pre-push reports the skip and
continues to the other checks. Findings, rejected credentials, and scanner failures still block.

## Coding-agent feedback

RuleSync generates a post-edit hook for Codex, Cursor, and Claude. The hook extracts edited
repository paths from each tool's event payload and runs a bounded, type-independent subset of the
generated SonarJS configuration, only on production `src/` files that `npm run lint` also analyzes.
Findings are printed near the edit and the hook exits unsuccessfully.

Edit `.rulesync/hooks.jsonc` or `scripts/agent-hooks/scan-edited-file.mjs`, then run
`npm run rules:sync`; do not edit generated tool hook files (`.cursor/hooks.json`,
`.claude/settings.json`, `.codex/hooks.json`) directly.

## SonarQube for IDE

The repository recommends the SonarQube for IDE extension and checks in the project binding. In VS
Code or Cursor, create a SonarQube connection whose ID is `https-sonar-casadega-dev`, point it at
<https://sonar.casadega.dev>, and bind using the project key in `.sonarlint/connectedMode.json`.
