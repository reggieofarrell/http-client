---
/**
 * Empty replacement for Nova's shared ThemeSelect slot.
 *
 * Nova places this slot in its desktop header and mobile menu footer. HTTP
 * Client uses a fixed dark palette, so rendering no control makes that policy
 * explicit without changing Nova's header layout or navigation behavior.
 */
---

<!-- The fixed-dark site intentionally exposes no theme selector. -->
